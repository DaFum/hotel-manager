# Commercial-domain conventions

- Contract validity uses half-open date ranges: `validFromDateKey <= date < validToDateKey`.
- Loyalty points and their ledger liability must remain reconcilable after earning, breakage, and burns.
