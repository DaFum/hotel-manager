# Shared-domain conventions

- Protocol correlation fields and publication numbers are required data, not optional fixture decoration.
- Structural comparisons distinguish missing keys from present `undefined` values and must not depend on object insertion order.
